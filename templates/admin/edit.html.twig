{% extends 'admin/base.html.twig' %}

{% block content %}


    <form action="{{ path('admin_edit', {'segment': segment, 'id': id}|merge(app.request.query.all)) }}" method="post" novalidate
          autocomplete="off">
        {{ form_errors(form) }}
        {{ form_rest(form) }}

        <a class="btn btn-danger" href="{{ path('admin_list', {'segment': segment}|merge(app.request.query.all)) }}">Cancel</a>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>

{% endblock %}