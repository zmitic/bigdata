{% extends 'base.html.twig' %}

{% block body %}
<table>
    <tr>
        <th class="sort {{ pager.direction }}">{{ knp_pagination_sortable(pager, 'Name', 'o.name') }} {{ pager.direction }}</th>
    </tr>

    {% for category in pager %}
        <tr>
            <td>{{ category }}</td>
        </tr>
    {% endfor %}
</table>


    {{ knp_pagination_render(pager) }}
{% endblock %}